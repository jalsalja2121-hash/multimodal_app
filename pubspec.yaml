name: multimodal_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  image_picker: ^1.1.2
  speech_to_text: ^7.0.0
  path_provider: ^2.1.5
  google_generative_ai: ^0.4.6
  ultralytics_yolo: ^0.0.8
  cloud_firestore: ^5.4.4
  firebase_core: ^3.6.0
  hive_ce: ^2.9.0
  hive_ce_flutter: ^2.2.0
  fl_chart: ^0.69.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true